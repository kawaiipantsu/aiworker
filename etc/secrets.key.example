This is documentation, not a usable encryption key. Do not copy it to secrets.key.

contrib/bootstrap-local.py generates etc/secrets.key as 32 random binary bytes,
owned by root:aiworker-credentials with mode 0640. For manual installation, use
INSTALL.md's exclusive-create Python command. Never commit the generated key.
