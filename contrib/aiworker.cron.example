# Optional log retention; review the dry-run report before installing.
# 17 4 * * * root /usr/bin/php /opt/aiworker/app/contrib/cleanup.php --apply --days=90
# Daily backups are provided by aiworker-backup.timer instead of cron.
# Daily suggestion queue (server timezone must be Europe/Copenhagen).
0 8 * * * aiworker /usr/bin/php /opt/aiworker/app/contrib/suggestions.php
